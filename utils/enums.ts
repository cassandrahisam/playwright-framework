// Helpful enums to use in multiple files

export enum TestUsers {
  Admin = "adminUser",
  Test = "testUser",
}

export enum Features {
  Login = "login",
  Dashboard = "dashboard",
  ContactUs = "contact",
}